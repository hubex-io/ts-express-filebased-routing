# ts-express-filebased-router

A minimal Express server with filebased routing, in Typescript.

# Usage

Start the development server: `npm run dev`

Build: `npm run build` & start production build: `npm run start`
