{
  "name": "ts-express-filebased-routing",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch --env-file=.env index",
    "build": "rm -rf ./dist && tsc",
    "start": "node --env-file=.env ./dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.19.2",
    "express-file-routing": "^3.0."
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.2",
    "tsx": "^4.15.2",
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=20.12.0"
  }
}
